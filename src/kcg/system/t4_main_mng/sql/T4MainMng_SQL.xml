<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace='kcg.system.t4_main_mng'>

	<select id="getList" parameterType="cmmnMap" resultType="cmmnMap">
		SELECT 
			*
		FROM 
		테이블명
	</select>
	
	 <insert id="newCreateManagement" parameterType="cmmnMap">
        INSERT INTO employees (
            user_id, user_pw, email, name, status_cd, jikgub_nm, dept, reg_dt
        ) VALUES (
            #{user_id}, #{user_pw}, #{email}, #{name}, #{status_cd}, #{jikgub_nm}, #{dept}, NOW()
        )
    </insert>

</mapper>